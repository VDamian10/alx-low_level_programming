#include "main.h"
/**
 * reverse_array - entry point
 * @a: array of integers
 * @n: number of elements of array
 *
*/
void reverse_array(int *a, int n)
{
	int o;

	for (o = n - 1; o >= 0; o--)
	{
		a[o];
	}
}
