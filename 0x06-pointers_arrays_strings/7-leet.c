#include "main.h"
/**
 * leet - entry point
 * @str: string
 * a function that encodes a string into leet
 *
 * Return: string
*/
char *leet(char *str)
{
	int o = 0;

	while (str[o] != '\0')
	{
		o++;
	}
}
