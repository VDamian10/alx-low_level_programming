#include <stdio.h>
/**
 * main - a line of code to print out the size of types on a computer
 *
 * Return: 0
*/
int main(void)
{
	char c;
	int i;
	double d;

	sizeof(c);
	sizeof(i);
	sizeof(d);

	return (0);
}
